# TEST
```ruby
hardware/            # ESP32 / Arduino hardware code
    ├── socket.client.cpp    # main connection code
    ├── sensors/             # optional, sensor-specific code later
    │   ├── ph_sensor.cpp
    │   └── temperature_sensor.cpp
    └── lib/                 # reusable libraries for ESP32 (WiFi, JSON, etc.)
```


https://mosquitto.org/download/